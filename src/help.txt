isspace(char c) - zkontroluje, jestli c neni mezera, pokud ano 1, pokud ne 0
struct CommandParameters - obsahuje parametry pøíkazu
std::set.count(Object o) - funkce pro spoèítání kolikrát se o vyskytuje v setu
std::string.find(char c, size_t position) - vrací size_t místa, kde se nachází hledaný znak od zvolené pozice. -1 pokud nenajde
std::string::npos - -1, taky znamená poslední char Stringu (èili do konce Stringu)
std::string.substr(size_t start, size_t end) - udìlá substring od pozice start do pozice end
std::smatch - instance match_result - používá se pro test na shodu
std::smatch.position() - vrací pozici posledního znaku v smatch objectu
std::vector.push_back(Object O) - dá O na konec vektoru (vèetnì alokace pamìti a pointeru)

